<!-- android/app/src/main/AndroidManifest.xml — GAMBIT TSL -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet — required for all API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- READ_EXTERNAL_STORAGE / READ_MEDIA_* — needed for file_picker (document upload) -->
    <!-- Android 13+ uses granular permissions; older versions use READ_EXTERNAL_STORAGE -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:label="Gonyeti TLS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"

        <!-- Accessibility: allow system font scaling (do not lock text size) -->
        android:supportsRtl="false"

        <!-- Cleartext traffic only to localhost for debug; prod uses HTTPS only -->
        android:usesCleartextTraffic="false"

        <!-- Required for file_picker on Android 11+ -->
        android:requestLegacyExternalStorage="false"

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"

            <!-- Edge-to-edge: let Flutter draw behind system bars -->
            android:windowSoftInputMode="adjustResize"

            <!-- Accessibility: respond to system font-size and display-size changes -->
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"

            android:hardwareAccelerated="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link: gambit://open (future use for push notification taps) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="gambit" android:host="open" />
            </intent-filter>
        </activity>

        <!-- FileProvider required for file_picker on Android 7+ -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>